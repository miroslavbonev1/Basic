package EasyToMedium;

import java.util.Scanner;

public class FootballResults {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        String firstGame = scanner.nextLine();
        String secondGame = scanner.nextLine();
        String thirdGame = scanner.nextLine();
        int win = 0;
        int lost = 0;
        int drawn = 0;
        //char valueFirst = firstGame.charAt(0);
        int firsty = firstGame.charAt(0);
       // int valueSecond = Integer.parseInt(secondGame);
        //if (first > second){
            win++;
        } //else if (first == second) {
            //drawn++;
        } //else {
            //lost++;
        //}
        //System.out.printf("Team won %d games.%n",win);
        //System.out.printf("Team lost %d games.%n",lost);
        //System.out.printf("Drawn games: %d%n",drawn);
    //}
//}
